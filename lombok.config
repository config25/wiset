# Jackson @JsonProperty 를 Lombok 이 생성하는 getter/setter 로 복사.
#   boolean 필드(isFinal 등)에서 Lombok 게터명과 Jackson 프로퍼티명이 어긋나는 문제 방지.
config.stopBubbling = true
lombok.copyableAnnotations += com.fasterxml.jackson.annotation.JsonProperty
